import { StyleSheet } from 'react-native';

const headerBarStyles = StyleSheet.create({
  searchBar: {
    flex: 1,
    width: '85%',
    height: 40,
    backgroundColor: '#FFFFFF',
  },

  headerColor: { backgroundColor: '#0E3361' },
});

export { headerBarStyles };
