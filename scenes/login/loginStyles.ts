import { StyleSheet } from 'react-native';

const loginStyles = StyleSheet.create({
  default: {
    justifyContent: 'center',
    alignItems: 'center',
    flex: 1,
  },
});

export { loginStyles };
